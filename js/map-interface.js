var Map = require('./../js/map.js').mapModule;

$(document).ready(function() {
  // var inputCity, inputState, location, lat, lng, myLatLng;
  // var newMap = new Map();
  //
  // $("#city-form").submit(function(event) {
  //   event.preventDefault();
  //   inputCity = $('#city').val();
  //   inputState = $('#state').val();
  //   location = newMap.getLocation(inputCity, inputState);
  //   geolocationSuccess(location);
  // });

});
